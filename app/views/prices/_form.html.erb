<%= form_for(price) do |f| %>
  <% if price.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(price.errors.count, "error") %> prohibited this price from being saved:</h2>

      <ul>
      <% price.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :component_id %>
    <%= f.text_field :component_id %>
  </div>

  <div class="field">
    <%= f.label :qty %>
    <%= f.number_field :qty %>
  </div>

  <div class="field">
    <%= f.label :cost_each %>
    <%= f.text_field :cost_each %>
  </div>

  <div class="field">
    <%= f.label :shipping_each %>
    <%= f.text_field :shipping_each %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
